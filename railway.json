{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "python -c \"print('STEP1_PYTHON_OK'); import sys; print('STEP2_SYS_OK'); import os; print('PORT='+str(os.environ.get('PORT','MISSING'))); print('DB='+str(os.environ.get('DATABASE_URL','NONE'))[:30]); print('STEP3_ENV_OK')\" && echo STEP4_SHELL_OK && exec uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
