# Core Framework
fastapi==0.110.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0
aiosqlite==0.20.0
greenlet==3.0.3

# Data Validation
pydantic==2.5.3
pydantic-settings==2.2.1

# Configuration
python-dotenv==1.0.0

# Authentication & Security
python-jose==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# HTTP & APIs
requests==2.31.0
httpx==0.26.0
aiohttp==3.9.3

# Telegram
aiogram==3.4.0

# RSS & News
feedparser==6.0.10

# Macro Intelligence — sector_impact_map.yaml loader
PyYAML==6.0.1

# Macro Intelligence — Fed SEP PDF parse (Faz B dot plot otomasyon)
pdfplumber==0.11.0

# BIST (Borsa Istanbul) data — yfinance backend (Phase 1)
# Phase 2 will swap to Twelve Data SDK ($29/mo, real-time)
yfinance==0.2.40
pandas==2.2.0
numpy==1.26.4

# Macro inline-keyboard charts — rendered server-side as PNG and uploaded
# via Telegram sendPhoto. Headless (Agg backend) on Railway.
matplotlib==3.8.4

# Stripe billing — /upgrade Telegram command opens a Stripe Checkout
# session keyed by telegram_id metadata; webhook flips users.tier on
# subscription.created / .updated / .deleted.
stripe==9.0.0

# CoinGlass ETF flow scraper — JS-rendered tables require a real browser.
# Replaces the unreliable .github/workflows/coinglass-etf-cron.yml; runs
# in services/coinglass_scheduler.py every 6h. Chromium installed via
# `playwright install --with-deps chromium` in the Dockerfile.
playwright==1.42.0

# Technical Analysis (Phase 2.2) - Install when TradingView API available
# pandas-ta==0.3.14b0
# mplfinance==0.12.9

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0

# Logging & Monitoring
python-json-logger==2.0.7

# Development
black==24.3.0
flake8==7.0.0
isort==5.13.2
mypy==1.8.0
email-validator==2.3.0
